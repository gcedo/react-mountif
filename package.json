{
  "name": "react-mountif",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf lib && mkdir lib && babel --loose --stage 0 --out-dir lib src",
    "examples": "npm run build && webpack --config examples/webpack.example.babel.js --progress",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm run build"
  },
  "keywords": [
    "react",
    "mount",
    "decorator"
  ],
  "author": "Edoardo Colombo <edo.gcolombo@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "react": "^0.13.3"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "babel-core": "^5.8.23",
    "babel-loader": "^5.3.2",
    "webpack": "^1.12.0"
  }
}
